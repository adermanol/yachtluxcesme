{
  "name": "yachtluxcesme",
  "private": true,
  "version": "0.1.0",
  "description": "YachtLux Çeşme — landing page + admin paneli",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "config": "node scripts/generate-config.js"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.1"
  }
}
